$(document).ready(function(){
    var now_offset_top = $(window).offset().top;
    var window_height = $(window).height();

    $(window).scroll(function(){
        if(now_offset_top < window_height){

        }
    })


})
